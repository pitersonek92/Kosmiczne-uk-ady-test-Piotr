// InlineImages — intentionally empty.
// All assets are served from the images/ folder via api.enginePath() (ZPE CDN).
// Do NOT add Base64 data here — it bloats entry.js and breaks offline mode on ZPE.
export const InlineImages: Record<string, string> = {};
